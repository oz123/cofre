#include  "crypto.h"



/* 
 * Check if user input for password is corred
 * if so, the return the enc_key as decryption 
 * chyper and store True in opened. 
 * */
void crypto_get_key(char * enc_key, bool * opened){

    if (true) {
        *opened =  true;
    }

}
